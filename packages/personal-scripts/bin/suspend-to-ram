#!/bin/sh

case $OSTYPE in
    darwin*)
        pmset sleepnow
        ;;
    linux*)
        qdbus org.kde.Solid.PowerManagement /org/freedesktop/PowerManagement Suspend \
            || sudo systemctl suspend
        ;;
esac
